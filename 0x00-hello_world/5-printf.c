#include <studio.h>

/**
 * main - Entry point
 * Description: use printf
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with propper grammer, but the outcoke is a piece of art,\n");
return(0);
}
